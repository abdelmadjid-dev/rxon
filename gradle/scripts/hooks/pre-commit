#!/bin/sh

echo "Running pre-commit release checks..."

# Run the standalone verification script
sh gradle/scripts/verify_release.sh

if [ $? -ne 0 ]; then
    echo "ERROR: Release verification failed. Commit aborted."
    exit 1
fi
