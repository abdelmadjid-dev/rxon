#!/bin/sh

echo "Running pre-commit release checks..."

# Run the standalone verification script from the local directory
sh "$(dirname "$0")/verify_release.sh"

if [ $? -ne 0 ]; then
    echo "ERROR: Release verification failed. Commit aborted."
    exit 1
fi
